/**
 * Placeholder content, in every shipped language, so the localisation path is
 * exercised out of the box. Replace all of it via `DemosceneOptions.content`.
 *
 * The actual copy lives one-file-per-language under `src/locales/` (see
 * [[../locales/index]]); this module just re-exports the assembled result so
 * existing imports of `DEFAULT_CONTENT` keep working unchanged.
 */
export { DEFAULT_CONTENT } from '../locales';
