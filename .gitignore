# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/

# Environment files
/.env

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Distribution directories
dist/
# The demoscene dev host's single-file IIFE build (escape hatch, never shipped).
dist-standalone/
.verify/

# Typescript v1 declaration files
typings/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# IntelliJ
*.iml
.idea/workspace.xml
.idea/tasks.xml
.idea/gradle.xml
.idea/assetWizardSettings.xml
.idea/dictionaries
.idea/libraries
# Android Studio 3 in .gitignore file.
.idea/caches
.idea/modules.xml
# Comment next line if keeping position of elements in Navigation Editor is relevant for you
.idea/navEditor.xml
apps/guitar-codex/.idea/
.orca/
prototypes/tetris/
pages/
pages.old/
images/
# One lockfile for the whole workspace, kept out of the tree exactly as
# app/package-lock.json used to be. `npm install` at the root regenerates it.
package-lock.json
# The odometer's reference photographs — leading slash so this matches ONLY the
# folder at the repository root. Written bare, it also matched
# `apps/guitar-codex/src/lib/counter/` and `.../components/counter/`, which silently kept
# the whole visitor-counter feature out of every commit.
/counter/
packages/guestbook/.env
.idea/vcs.xml
pages.partial/
