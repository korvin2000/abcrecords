# Keep development resource URLs identical to production. Vite proxies this
# path to the legacy archive because the repository contains catalogue data
# but not every referenced score scan, recording, or document.
VITE_RESOURCE_BASE_PATH=/pages
