# Dev only. In production the catalogue content is hosted at <domain>/pages,
# so VITE_RESOURCE_BASE_PATH defaults to "/pages" (see src/lib/paths.ts). In
# dev, Vite serves publicDir (../pages) from the site root, so resources
# referenced inside biographies/metadata (music, images) resolve from "/".
VITE_RESOURCE_BASE_PATH=/
