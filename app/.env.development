# Keep development resource URLs identical to production. The legacy-archive
# dev plugin (vite/legacy-archive.ts) serves anything publicDir does not have
# from the live site, because the repository contains catalogue data but not
# every referenced score scan, recording, or document.
VITE_RESOURCE_BASE_PATH=/pages
